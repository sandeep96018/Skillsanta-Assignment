const set = new Set(['Beethoven', 'Mozart', 'Chopin', 'Chopin'])
set.delete('Beethoven');
console.log(set);

/* we can simply delete the elements from set with help of delete() method
 after deleting the value 'Beethoven' the new set become 'mozrat' and 'Chopin' because
 set contains unique value.
 */